#   Global Static IP Module

Creates reusable Global static IP addresses for GCP.

## Features 

- Multiple global IP addresses
- IPv4 or IPv6 support
- Terraform `for_each`
- Outputs IP addresses and self-links

## Use Case

The reserved global IPv4 address can be attached to a
Google Cloud external HTTPS Load Balancer.